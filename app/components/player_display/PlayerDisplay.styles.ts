import {circle, hstack} from '@styles/patterns';

export const cPlayerDisplay_display = hstack({
  gap: 4,
  bg: 'platinum.700',
  borderRadius: 'md',
  p: 4,
  w: '15vw',
  fontSize: 'lg',
});

export const cPlayerDisplay_picture = circle({
  size: 14,
});
